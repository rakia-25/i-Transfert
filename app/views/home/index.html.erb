<% if user_signed_in? %>
  <p class="font-semibold text-xl text-gray-800 mb-6">
    Bienvenue <%= current_user.name %>
  </p>
  <%= form_with url: destroy_user_session_path, method: :delete do %>
    <button type="submit"
              class="bg-red-600 text-white py-2 px-6 rounded-lg shadow hover:bg-red-700 focus:outline-none focus:ring-4 focus:ring-red-300 transition duration-200">
      Déconnexion
    </button>
  <% end %>
<% else %>
  <h1 class="text-4xl font-bold mb-4 text-indigo-700 text-center">
    Bienvenue sur mon blog
  </h1>
  <%= link_to "Se connecter", new_user_session_path, class: "bg-green-600 text-white py-2 px-4 rounded-lg shadow hover:bg-green-700 focus:outline-none focus:ring-4 focus:ring-green-300 transition duration-200" %>
<% end %>