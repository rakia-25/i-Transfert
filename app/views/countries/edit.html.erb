<h1>Modifier le pays</h1>

<%= form_with(model: @country) do |f| %>
  <div>
    <%= f.label :name, "Nom du pays" %>
    <%= f.text_field :name %>
  </div>

  <%= f.submit "Mettre à jour" %>
<% end %>

<%= link_to "Annuler", country_path(@country) %>
