<h1>Bienvenue sur la  page de la liste des Historiques</h1>
<% if notice %>
  <div class="bg-green-100 text-green-800 border border-green-200 px-4 py-2 rounded mb-4">
    <%= notice %>
  </div>
<% end %>
<div class="bg-white my-10 mx-10  shadow-teal-950 shadow-xl   py-10 px-10 rounded-[15px] w-[1000px] ">
    <div class="flex ">
       <h1 class="font-bold  text-[35px] text-teal-700/[0.5]">Liste Des Historiques</h1>
    </div>
       <hr>
    <div class="overflow-x-auto rounded  my-15 border border-gray-300 shadow-sm">
      <table class="min-w-full divide-y-2 divide-gray-200">
        <thead class="ltr:text-left rtl:text-right">
          <tr class="*:font-medium *:text-gray-900">
            <th class="px-3 py-2 whitespace-nowrap">Guichet</th>
            <th class="px-3 py-2 whitespace-nowrap">Solde Avant</th>
            <th class="px-3 py-2 whitespace-nowrap">Solde Après</th>
            <th class="px-3 py-2 whitespace-nowrap">Type Transaction</th>
          </tr>
        </thead>

        <tbody class="divide-y divide-gray-200">
        <% if @historicals.empty? %>
            <tr class="text-red ">Aucun article trouvé.</tr>
          <% else %>
          <% @historicals.each do |historical| %>
            <tr class="*:text-gray-900 *:first:font-medium">
              <td class="px-3 py-2 whitespace-nowrap"><%= historical.guichet.name %></td>
              <td class="px-3 py-2 whitespace-nowrap"><%= historical.balance_before %></td>
              <td class="px-3 py-2 whitespace-nowrap"><%= historical.balance_after %></td>
              <td class="px-3 py-2 whitespace-nowrap"><%= historical.operation_type %></td>
            </tr>
          <% end %>
          <% end %>
        </tbody>
      </table>
  </div>
</div>

